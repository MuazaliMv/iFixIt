'use client';

import { useEffect, useMemo, useState } from 'react';
import { supabase } from '../../lib/supabaseClient';

const API='https://yzlhlilxiszefneshatm.supabase.co/functions/v1/customer-requests';
type RequestRow={id:string;ticket_number:string;service_name:string;service_category_code:string;service_location_text:string;preferred_date:string;problem_description:string;status:string;assigned_provider_label?:string|null;created_at:string;updated_at:string};
type HistoryRow={from_status?:string|null;to_status:string;actor_type?:string|null;note?:string|null;created_at:string};
type Filter='ACTIVE'|'COMPLETED'|'ALL';
const workflow=['NEW','ACCEPTED','PROCESSING','COMPLETED'];

export default function MyRequestsPage(){
 const[requests,setRequests]=useState<RequestRow[]>([]);const[selected,setSelected]=useState<RequestRow|null>(null);const[history,setHistory]=useState<HistoryRow[]>([]);const[filter,setFilter]=useState<Filter>('ACTIVE');const[busy,setBusy]=useState(false);const[message,setMessage]=useState('Loading your requests…');
 useEffect(()=>{void load();},[]);
 async function token(){const{data}=await supabase.auth.getSession();if(!data.session){window.location.href='/login';return '';}return data.session.access_token;}
 async function call(body:Record<string,unknown>){const t=await token();if(!t)throw new Error('Sign in required');const r=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${t}`},body:JSON.stringify(body)});const p=await r.json();if(!r.ok)throw new Error(p?.error||'Request history failed');return p;}
 async function load(){setBusy(true);try{const p=await call({action:'list'});setRequests(p.requests||[]);setMessage((p.requests||[]).length?'Your request history is up to date.':'You have no service requests yet.');}catch(e){setMessage(e instanceof Error?e.message:'Unable to load requests.');}finally{setBusy(false);}}
 async function openRequest(row:RequestRow){setSelected(row);setHistory([]);setBusy(true);try{const p=await call({action:'detail',ticketNumber:row.ticket_number});setSelected(p.request||row);setHistory(p.history||[]);}catch(e){setMessage(e instanceof Error?e.message:'Unable to load request detail.');}finally{setBusy(false);}}
 const visible=useMemo(()=>requests.filter(r=>filter==='ALL'||(filter==='COMPLETED'?r.status==='COMPLETED':r.status!=='COMPLETED')),[requests,filter]);
 const counts=useMemo(()=>({all:requests.length,active:requests.filter(r=>r.status!=='COMPLETED').length,completed:requests.filter(r=>r.status==='COMPLETED').length}),[requests]);
 const activeStep=selected?Math.max(0,workflow.indexOf(selected.status)):0;
 return <main className="shell">
  <header className="topbar"><div><a className="brand" href="/">FixIt</a><p className="tagline">My Requests</p></div><div className="actions"><a className="secondary" href="/">New Request</a><button className="secondary" disabled={busy} onClick={()=>void load()}>Refresh</button></div></header>
  <section className="panel"><div className="panelHeader"><div><p className="eyebrow">CUSTOMER DASHBOARD</p><h2>All your service requests</h2></div><span className="pill">{counts.active} active</span></div><div className="adminStats"><div className="statCard"><span>All</span><strong>{counts.all}</strong></div><div className="statCard"><span>Active</span><strong>{counts.active}</strong></div><div className="statCard"><span>Completed</span><strong>{counts.completed}</strong></div></div>{message?<p className="formMessage" role="status">{message}</p>:null}<div className="filterRow"><button className={filter==='ACTIVE'?'filterChip active':'filterChip'} onClick={()=>setFilter('ACTIVE')}>Active</button><button className={filter==='COMPLETED'?'filterChip active':'filterChip'} onClick={()=>setFilter('COMPLETED')}>Completed</button><button className={filter==='ALL'?'filterChip active':'filterChip'} onClick={()=>setFilter('ALL')}>All</button></div></section>
  <section className="panel"><div className="panelHeader"><div><p className="eyebrow">REQUESTS</p><h2>{filter==='ACTIVE'?'Active requests':filter==='COMPLETED'?'Completed requests':'Request history'}</h2></div></div><div className="jobList">{visible.map(r=><button key={r.id} type="button" className="jobCard" style={{textAlign:'left',width:'100%'}} onClick={()=>void openRequest(r)}><div className="jobTop"><div><strong className="ticket">{r.ticket_number}</strong><span>{r.service_name}</span></div><span className="pill">{r.status}</span></div><div className="jobMeta"><span>{r.service_location_text}</span><span>{r.preferred_date}</span>{r.assigned_provider_label?<span>{r.assigned_provider_label}</span>:null}</div><p className="jobDescription">{r.problem_description}</p></button>)}{!visible.length?<div className="emptyQueue">No requests in this section.</div>:null}</div></section>
  {selected?<section className="panel"><div className="panelHeader"><div><p className="eyebrow">REQUEST DETAIL</p><h2>{selected.ticket_number}</h2></div><button className="secondary" onClick={()=>{setSelected(null);setHistory([]);}}>Close</button></div><div className="trackingGrid"><div className="requestSummary"><p><b>Service:</b> {selected.service_name}</p><p><b>Location:</b> {selected.service_location_text}</p><p><b>Preferred date:</b> {selected.preferred_date}</p><p><b>Provider:</b> {selected.assigned_provider_label||'Not assigned yet'}</p><p><b>Status:</b> {selected.status}</p><p><b>Created:</b> {new Date(selected.created_at).toLocaleString()}</p></div><div className="miniTimeline">{workflow.map((item,index)=><div className={index<=activeStep?'timelineItem done':'timelineItem'} key={item}><span>{index+1}</span><div><strong>{item[0]+item.slice(1).toLowerCase()}</strong><small>{index<activeStep?'Completed stage':index===activeStep?'Current stage':'Pending'}</small></div></div>)}</div></div>{history.length?<div className="jobList">{history.map((h,index)=><div className="jobCard" key={`${h.created_at}:${index}`}><strong>{h.from_status||'START'} → {h.to_status}</strong><p className="jobDescription">{h.note||'Status updated'}</p><span className="muted">{new Date(h.created_at).toLocaleString()}</span></div>)}</div>:null}</section>:null}
  <footer className="footer"><span>FixIt Maldives</span><span>Authenticated Request History</span></footer>
 </main>;
}
