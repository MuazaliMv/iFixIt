Trigger Railway deployment from the latest main head only.
Updated: 2026-08-25T03:24:00+05:00
Reason: deploy the unified production design system across Admin, Service Provider, and Customer workspaces, including the latest Provider and Customer design-system merge.

Required production behavior:
- Production source of truth is GitHub branch: main.
- Every production deployment must build from the current main HEAD.
- Do not roll back, redeploy, promote, or pin an older deployment unless explicitly requested by the project owner.
- Build a fresh Next.js production bundle from the selected commit.
- ifixmv.com must point to the newest successful production deployment.

Current target main HEAD at time of this trigger:
- bf27675a077fc2c2ed48698ca87eb91453a73296
- Includes PR #69 Admin design system and PR #70 Provider + Customer design system.

Expected production UI behavior:
- Admin keeps the simplified five-group navigation with submenus and scoped light/dark design system.
- Customer and Service Provider workspaces use the same login-aligned semantic design language, responsive surfaces, rounded controls/cards, 48px touch targets, and scoped light/dark appearance.
- Existing live Supabase-backed customer requests, provider jobs/offers, admin operations, role checks, permissions, and route protection remain unchanged.
- Mobile-first behavior remains enabled across all workspaces.

This trigger intentionally changes on main so the Railway hosting integration performs a new production build from the latest source.
