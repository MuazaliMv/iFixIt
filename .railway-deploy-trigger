Trigger Railway deployment from the latest main head only.
Updated: 2026-08-25T11:19:00+05:00
Reason: force a fresh production deployment after deep deployment audit and ensure Railway builds the actual current main HEAD.

Required production behavior:
- Production source of truth is GitHub branch: main.
- Every production deployment must build from the current main HEAD.
- Do not roll back, redeploy, promote, or pin an older deployment unless explicitly requested by the project owner.
- Build a fresh Next.js production bundle from the selected commit.
- ifixmv.com must point to the newest successful production deployment.
- Railway must run the repository Dockerfile and start the Next.js server with npm start.

Current target main HEAD at time of this trigger:
- a1a54bcdef9028a72de3885af4d412bf05ad36fb
- Includes the latest merged mobile profile save fix and prior iOS-style web-app usability changes.

Deployment audit findings:
- GitHub Build workflow for current main HEAD completed successfully.
- npm install completed successfully.
- npm run build completed successfully.
- Dockerfile is valid for the Next.js production build and exposes port 3000.
- railway.toml uses the Dockerfile builder and npm start.
- The prior deployment trigger referenced an older main commit and has now been refreshed.

Expected production behavior:
- Railway detects this new main commit and creates a fresh production deployment.
- The deployment must use the newest GitHub main source, not a cached or older revision.
- Existing Customer, Provider, and Admin functionality must remain unchanged.

If the new Railway deployment succeeds but ifixmv.com is still unreachable, the remaining fault is outside the application codebase and must be corrected in Railway domain/DNS configuration.
