Trigger Railway deployment from the latest main head only.
Updated: 2026-08-25T03:32:00+05:00
Reason: deploy the Service Provider workspace routing fix so selecting Service Provider never falls back to the public marketing homepage.

Required production behavior:
- Production source of truth is GitHub branch: main.
- Every production deployment must build from the current main HEAD.
- Do not roll back, redeploy, promote, or pin an older deployment unless explicitly requested by the project owner.
- Build a fresh Next.js production bundle from the selected commit.
- ifixmv.com must point to the newest successful production deployment.

Current target main HEAD at time of this trigger:
- 8b2d2a6a50a28e79001ea4948f94dc347ceff662
- Includes PR #71 Provider workspace routing fix on top of the unified Admin, Provider, and Customer design systems.

Expected production routing behavior:
- Selecting Service Provider from the authenticated account/workspace menu opens the Provider workspace.
- An account authorised for Provider access must not be redirected to the public `/` marketing landing page because onboarding/approval metadata is incomplete or temporarily unavailable.
- Server-side role access in proxy.ts remains authoritative.
- Subscription routing to `/provider/subscription` remains supported when applicable.
- Customer and Admin routing and permissions remain unchanged.

Validation before merge:
- Regression test for Provider routing passed.
- iFixIt CI passed tests, lint, and production build.
- Separate Build workflow passed.

This trigger intentionally changes on main so the Railway hosting integration performs a new production build from the latest source.
