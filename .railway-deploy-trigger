Trigger Railway deployment from the latest main head only.
Updated: 2026-08-25T03:50:00+05:00
Reason: deploy the verified pre-production usability hardening, including the unified account profile, separate Provider verification flow, improved profile accessibility/mobile behavior, and server/client logout synchronization.

Required production behavior:
- Production source of truth is GitHub branch: main.
- Every production deployment must build from the current main HEAD.
- Do not roll back, redeploy, promote, or pin an older deployment unless explicitly requested by the project owner.
- Build a fresh Next.js production bundle from the selected commit.
- ifixmv.com must point to the newest successful production deployment.

Current target main HEAD at time of this trigger:
- f84d63872f308b7f5388c545b7738f8d78621407
- Includes PR #72 unified profile/settings redesign and PR #73 pre-deploy usability hardening.

Expected production usability behavior:
- Customer, Provider, and Admin accounts use one real account profile/settings experience at `/profile`.
- `/provider/profile` remains a compatibility route and opens the unified account profile instead of a verification-only screen.
- Provider verification documents and subscription controls remain available separately at `/provider/verification` and `/provider/subscription`.
- Profile forms use mobile-safe 16px inputs, 48px touch targets, improved autofill, keyboard focus visibility, anchor offsets, and live save-status feedback.
- Client-side sign out also clears the HttpOnly server session through `/api/auth/logout`.
- Existing role protection, Provider workspace routing, customer requests, provider jobs, and Admin operations remain unchanged.

Validation before deployment:
- PR #73 iFixIt CI passed tests, lint, and production build.
- PR #73 separate Build workflow passed.
- Regression tests cover Provider profile routing, Provider verification access, and server-session logout synchronization.

This trigger intentionally changes on main so the Railway hosting integration performs a new production build from the latest verified source.
