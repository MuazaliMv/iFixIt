Trigger Railway deployment from the latest main head only.
Updated: 2026-08-27T23:17:00+05:00
Reason: production provider approval screen is serving a stale UI where the Approve Provider action does not execute; redeploy latest main with the current provider-admin action wiring and mobile tap-target fixes.

Required production behavior:
- Production source of truth is GitHub branch: main.
- Every production deployment must build from the current main HEAD.
- Do not roll back, redeploy, promote, or pin an older deployment unless explicitly requested by the project owner.
- Build a fresh Next.js production bundle from the selected commit.
- ifixmv.com must point to the newest successful production deployment.
- Railway must run the repository Dockerfile and start the Next.js server with npm start.

Provider approval production requirements:
- Admin Provider Record must use the current main implementation in app/admin/providers/[userId]/page.tsx.
- Approve Provider must execute the authenticated admin-operations action and refresh the provider record after success.
- Reject and Suspend must remain functional.
- The obsolete disabled Submitted action tile must not be rendered as an action button.
- Mobile tap targets must remain interactive above global navigation overlays.
- Preserve the latest provider/admin UI styling and all current authentication and role rules.

Latest main commits to include:
- 273882d966aa144b00015f38dd8d1ad7dc9f7f02 — Fix provider approval button tap target on mobile
- 78e431a00b720974d922d01ed54d9d4225e7cd28 — Show provider application context in top workspace label

Expected production behavior:
- Railway detects this main commit and creates a fresh production deployment.
- ifixmv.com serves the latest main build, not the stale provider approval screen currently visible.
- Provider approval changes SUBMITTED -> APPROVED and synchronizes provider_approved/role through the existing admin-operations backend.
