Trigger Railway deployment from the latest main head only.
Updated: 2026-08-26T13:02:00+05:00
Reason: deploy the approved customer Service Request Details mobile UI model shown to the project owner.

Required production behavior:
- Production source of truth is GitHub branch: main.
- Every production deployment must build from the current main HEAD.
- Do not roll back, redeploy, promote, or pin an older deployment unless explicitly requested by the project owner.
- Build a fresh Next.js production bundle from the selected commit.
- ifixmv.com must point to the newest successful production deployment.
- Railway must run the repository Dockerfile and start the Next.js server with npm start.

Current target main HEAD at time of this trigger:
- 4efd5dab70841e1965d135134746505b5f1929c6
- Matches the approved mobile request detail model with Back to requests, Overview / Activity / Messages tabs, compact hero status, horizontal request progress, compact request detail rows, photos, and secondary cancel action.

Expected production behavior:
- Railway detects this new main commit and creates a fresh production deployment.
- The live Service Request Details screen must use the new approved mobile UI model.
- Existing request workflow, provider assignment, messaging, cancellation rules, and global navigation must remain functional.

If Railway completes successfully but ifixmv.com still shows the previous request details UI, verify the Railway service source commit, branch selection, build cache, and production domain target.