Trigger Railway deployment from the latest main head only.
Updated: 2026-08-26T14:21:00+05:00
Reason: deploy the new customer home usability screen after removing obsolete conflicting CSS layers.

Required production behavior:
- Production source of truth is GitHub branch: main.
- Every production deployment must build from the current main HEAD.
- Do not roll back, redeploy, promote, or pin an older deployment unless explicitly requested by the project owner.
- Build a fresh Next.js production bundle from the selected commit.
- ifixmv.com must point to the newest successful production deployment.
- Railway must run the repository Dockerfile and start the Next.js server with npm start.

Current target main HEAD at time of this trigger:
- db148327457aac05bef0a790545ecaece164ebc7
- Includes the new customer home usability CSS and removal of obsolete conflicting customer-home CSS files and imports.

Expected production behavior:
- Railway detects this new main commit and creates a fresh production deployment.
- The live customer home screen must show the new usability model rather than the older conflicting layout.
- Existing request wizard, provider assignment, messaging, cancellation rules, and global navigation must remain functional.

If Railway completes successfully but ifixmv.com still shows the previous customer home UI, verify the Railway service source commit, branch selection, build cache, and production domain target.